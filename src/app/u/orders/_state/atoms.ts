// import { atom } from 'jotai';
// import type { MenuItem } from '~/domain/menu-items';
// import type { PublicOrderWithItems } from '~/domain/orders';

// export const openOrdersAtom = atom<PublicOrderWithItems[]>([]);
// export const completedOrdersAtom = atom<PublicOrderWithItems[]>([]);
// export const menuItemsAtom = atom<Map<number, MenuItem>>(new Map());
